# Multi-Agent Development System

AI 에이전트들이 협업하여 소프트웨어 개발을 자동화하는 시스템입니다.

## 시스템 구조

```
orchestrator (중앙 제어)
    ├─> requirement-analyst  (요구사항 분석)
    ├─> ux-designer         (UX 설계)
    ├─> tech-architect      (기술 아키텍처)
    ├─> planner            (구현 계획)
    ├─> test-designer      (테스트 설계)
    ├─> developer          (코드 구현)
    ├─> reviewer           (코드 리뷰)
    └─> documenter         (문서화)
```

## 빠른 시작

### 1. 시스템 실행

```bash
# 실행 권한 부여
chmod +x run.sh run-auto.sh run-existing.sh scripts/*.sh

# 시스템 시작 (모드 선택)
./run.sh              # 일반 모드 (수동 승인 필요)
./run-auto.sh         # 완전 자동화 모드 (⚠️ 주의)
./run-existing.sh     # 기존 프로젝트 모드 (간소화)
```

### 2. 실행 모드

| 모드 | 스크립트 | 에이전트 | 용도 |
|------|---------|---------|------|
| 일반 | `run.sh` | 9개 전체 | 새 프로젝트 (수동 승인) |
| 자동화 | `run-auto.sh` | 9개 전체 | 새 프로젝트 (완전 자동) |
| 기존 프로젝트 | `run-existing.sh` | 4개만 | 기능 추가/버그 수정 |

### 3. 사용 방법

1. 스크립트 실행 후 오케스트레이터 세션에 접속됩니다
2. 프로젝트 유형 선택:
   - **1: 새 프로젝트** - 전체 워크플로우 (요구사항 분석 → 문서화)
   - **2: 기존 프로젝트** - 간소화 워크플로우 (바로 구현)
   - **3: 버그 수정** - Developer가 분석 후 수정
   - **4: 리팩토링** - 기존 코드 개선
3. 프로젝트 설명 또는 경로를 입력하세요

### 4. TDD (Test-Driven Development)

이 시스템은 **TDD 방식**을 강제합니다:
- Developer는 테스트 없이 코드를 작성하지 않습니다
- Reviewer는 테스트 없는 코드를 반려합니다
- 테스트 커버리지 70% 이상 권장

### 5. 세션 관리

```bash
# 모든 에이전트를 한 화면에 표시 (3x3 그리드)
bash scripts/view-all-agents.sh

# 모든 세션 보기
tmux list-sessions

# 특정 세션 접속
tmux attach-session -t [세션명]

# 세션 전환 (tmux 내부)
Ctrl+b, s

# 세션에서 나오기 (종료하지 않고)
Ctrl+b, d

# Pane 전체화면 토글 (view-all-agents 사용 시)
Ctrl+b, z

# 초기 단계 에이전트만 종료 (구현 단계 진입 후)
bash scripts/cleanup-phase.sh --initial

# 모든 세션 종료
bash scripts/stop-all.sh
```

### 4. 멀티 뷰 모니터링

`view-all-agents.sh`를 사용하면 9개 에이전트를 동시에 모니터링할 수 있습니다:

```bash
bash scripts/view-all-agents.sh
```

특징:
- 🎯 1초마다 각 에이전트의 최근 30줄 출력 표시
- 📊 3x3 타일 레이아웃으로 모든 에이전트를 한눈에 확인
- 🔍 `Ctrl+B, z`로 특정 pane 확대/축소
- 👁️ 읽기 전용 모드 (실수로 입력하는 것 방지)

각 pane에 표시되는 에이전트:
1. 🎯 Orchestrator - 중앙 제어
2. 📋 Requirements - 요구사항 분석
3. 🎨 UX Designer - UX 설계
4. 🏗️ Tech Architect - 기술 아키텍처
5. 📊 Planner - 구현 계획
6. 🧪 Test Designer - 테스트 설계
7. 💻 Developer - 코드 구현
8. 👀 Reviewer - 코드 리뷰
9. 📚 Documenter - 문서화

## 디렉토리 구조

```
multi-agent-system/
├── run.sh                      # 메인 실행 스크립트
├── scripts/
│   ├── setup-workspace.sh      # 워크스페이스 초기화
│   ├── setup-agents.sh         # 에이전트 설정
│   ├── cleanup-sessions.sh     # 세션 정리
│   ├── start-sessions.sh       # 세션 시작
│   ├── view-all-agents.sh      # 모든 에이전트 한 화면에 표시
│   └── stop-all.sh            # 전체 종료
└── workspace/
    ├── agents/                 # 각 에이전트 작업 디렉토리
    │   ├── orchestrator/
    │   │   └── CLAUDE.md      # 역할 정의
    │   ├── requirement-analyst/
    │   ├── ux-designer/
    │   └── ...
    ├── agents/                 # 각 에이전트 작업 디렉토리
    ├── artifacts/              # 생성된 산출물 (중간 문서)
    ├── tasks/                 # 에이전트별 작업 큐
    ├── signals/               # 통신용 시그널
    ├── status/                # 에이전트 상태
    ├── logs/                  # 실행 로그
    ├── input/                 # 사용자 입력
    ├── output/                # 최종 결과물 (프로젝트별 폴더)
    │   ├── 20241127_153000_todo-app/
    │   ├── 20241127_160000_dice-game/
    │   └── ...
    ├── tests/                 # 임시 테스트 파일
    ├── docs/                  # 임시 문서
    ├── src/                   # 임시 소스 코드
    └── project/               # 임시 작업 공간 (매 실행시 초기화)
```

## 개발 워크플로우

### Phase 0: 요구사항 분석
- **Agent**: requirement-analyst
- **작업**: 사용자 요청을 분석하고 불명확한 부분 질문
- **산출물**: requirements-draft.md → requirements.md

### Phase 1: UX 설계
- **Agent**: ux-designer
- **작업**: 사용자 플로우와 인터페이스 설계
- **산출물**: ux-design.md

### Phase 2: 기술 아키텍처
- **Agent**: tech-architect
- **작업**: 기술 스택 선정 및 아키텍처 설계
- **산출물**: tech-spec.md

### Phase 3: 구현 계획
- **Agent**: planner
- **작업**: 개발을 단계별 Iteration으로 분할
- **산출물**: implementation-plan.md

### Phase 4: 테스트 설계
- **Agent**: test-designer
- **작업**: TDD 방식으로 테스트 먼저 작성
- **산출물**: test-plan.md, tests/

### Phase 5: 구현 (반복)
- **Agent**: developer
- **작업**: 계획에 따라 코드 작성
- **산출물**: src/
- 각 Iteration 완료 후 reviewer가 검토

### Phase 6: 코드 리뷰
- **Agent**: reviewer
- **작업**: 코드 품질 검증
- **산출물**: review-comments.md

### Phase 7: 문서화
- **Agent**: documenter
- **작업**: 프로젝트 문서 생성
- **산출물**: README.md, ARCHITECTURE.md, API.md

## 통신 메커니즘

### 작업 지시 (Orchestrator → Agent)

```bash
# 작업 파일 생성
cat > /workspace/tasks/[agent-name]/task-001.json << EOF
{
  "task_id": "req-analysis-001",
  "command": "analyze_requirements",
  "input": "/workspace/input/user_request.txt",
  "output": "/workspace/artifacts/requirements.md",
  "callback": "/workspace/signals/req-analysis-done"
}
EOF

# tmux로 알림
tmux send-keys -t [agent-name]:0 "새 작업 할당됨" Enter
```

### 작업 완료 보고 (Agent → Orchestrator)

```bash
# 시그널 파일 생성
cat > /workspace/signals/req-analysis-done << EOF
status:completed
artifact:/workspace/artifacts/requirements.md
timestamp:2024-01-15T10:00:00Z
EOF
```

## 에이전트 상태

각 에이전트는 다음 상태 중 하나:
- `idle`: 대기 중
- `working`: 작업 중
- `waiting_approval`: 사용자 승인 대기
- `completed`: 작업 완료
- `error`: 오류 발생

상태 확인:
```bash
cat /workspace/status/[agent-name].status
```

## 트러블슈팅

### 세션이 시작되지 않는 경우

```bash
# 기존 세션 모두 종료
bash scripts/stop-all.sh

# 다시 시작
./run.sh
```

### 특정 에이전트만 재시작

```bash
# 해당 세션 종료
tmux kill-session -t [agent-name]

# 수동 재시작
tmux new-session -d -s [agent-name] -c workspace/agents/[agent-name]
tmux send-keys -t [agent-name]:0 "claude -p CLAUDE.md" Enter
```

### 작업이 멈춘 경우

```bash
# 에이전트 상태 확인
cat workspace/status/*.status

# 로그 확인
cat workspace/logs/orchestrator.log

# 시그널 확인
ls workspace/signals/
```

## 개발 예시

### 예시 1: 간단한 웹 앱

```
사용자: "TODO 리스트 애플리케이션 만들어줘"

→ requirement-analyst: 기능 질문 (추가/삭제/완료 표시?)
→ ux-designer: 리스트 UI 설계
→ tech-architect: React + localStorage
→ planner: 3 iterations
→ test-designer: 테스트 작성
→ developer: 구현 (각 iteration마다 reviewer 검토)
→ documenter: 문서화

결과: workspace/output/20241127_153000_todo/ 에 완성된 프로젝트
```

### 예시 2: 복잡한 3D 앱

```
사용자: "3D 주사위 굴리기 웹 만들어줘"

→ requirement-analyst: 상세 질문 (물리 엔진? 개수? 종류?)
→ ux-designer: 3D 뷰포트 + 컨트롤 패널
→ tech-architect: Three.js + Cannon.js + React Three Fiber
→ planner: MVP → 물리 → 폴리싱
→ test-designer: 물리 시뮬레이션 테스트
→ developer: 단계별 구현
→ reviewer: 성능 검증
→ documenter: 기술 문서

결과: workspace/output/20241127_160000_3d-/ 에 완전히 작동하는 3D 주사위 웹 앱
```

## 프로젝트 결과물 관리

### output 폴더 구조

각 프로젝트는 별도 폴더에 저장됩니다:

```
workspace/output/
├── 20241127_153000_todo-app/       # 첫 번째 프로젝트
│   ├── package.json
│   ├── src/
│   ├── public/
│   └── README.md
├── 20241127_160000_dice-game/      # 두 번째 프로젝트
│   ├── package.json
│   ├── src/
│   └── ...
└── 20241128_100000_dashboard/      # 세 번째 프로젝트
    └── ...
```

### 프로젝트 실행

```bash
# 완성된 프로젝트로 이동
cd workspace/output/20241127_153000_todo-app/

# 의존성 설치 (이미 되어 있을 수 있음)
npm install

# 개발 서버 실행
npm run dev
```

## 주요 기능

✅ **완전 자동화**: 사용자는 초기 요청과 중간 승인만 제공
✅ **체계적 워크플로우**: 맥도날드식 표준화된 개발 프로세스
✅ **품질 보증**: 각 단계마다 검증 및 리뷰
✅ **실시간 모니터링**: tmux 세션으로 진행 상황 확인 가능
✅ **문서 자동 생성**: README, API 문서 등 자동 작성

## 제한사항

- 각 에이전트는 Claude AI로 실행되므로 API 비용 발생
- 복잡한 프로젝트는 여러 시간 소요 가능
- 네트워크 연결 필요

## 라이선스

MIT License

## 기여

이슈나 개선 제안은 환영합니다!
